apiVersion: v1
kind: Service
metadata:
  name: {{ include "sbl-api.fullname" . }}
  labels:
    {{- include "sbl-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sbl-api.selectorLabels" . | nindent 4 }}
